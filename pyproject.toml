[project]
name = "jk-soccer-core"
version = "1.4.0"
description = "The core soccer module for jedi-knights."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [ "marimo", "matplotlib", "numpy", "pandas", "pydantic", "toml",]
[[project.authors]]
name = "Omar Crosby"
email = "omar.crosby@gmail.com"

[build-system]
requires = [ "hatchling",]
build-backend = "hatchling.build"

[dependency-groups]
dev = [ "mypy", "invoke", "pytest", "pytest-mock", "pytest-cov", "pytest-xdist", "ruff", "python-commitlint",]

[project.scripts]
jk-soccer-core = "jk_soccer_core:main"

[tool.invoke]
version = "2.2.0"
